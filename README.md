Pré requisitos 

Docker
Docker-compose
composer
php

----------------------------------------------
Docker

sudo docker-compose up -d --build 
----------------------------------------------
Laravel

composer install 
php artisan key:generate 
php artisan migrate

